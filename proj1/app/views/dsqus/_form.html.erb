<%= form_for(@dsqu) do |f| %>
  <% if @dsqu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dsqu.errors.count, "error") %> prohibited this dsqu from being saved:</h2>

      <ul>
      <% @dsqu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :issue_id %><br>
    <%= f.number_field :issue_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
